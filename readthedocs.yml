version: 2

build:
  os: ubuntu-20.04
  python:
    version: 3.8
  dependencies:
    python:
      - pip
      - setuptools
  install:
    - requirements: docs/requirements.txt
  pythonpath: docs
  sphinx:
    configuration: docs/source/conf.py
